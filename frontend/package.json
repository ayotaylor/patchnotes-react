{
  "name": "frontend",
  "private": true,
  "scripts": {
    "sync": "node sync-shared.js",
    "sync:watch": "node sync-shared.js --watch",
    "web": "npm run sync && cd web && npm start",
    "mobile": "npm run sync && cd mobile && npm start",
    "android": "npm run sync && cd mobile && npm run android",
    "ios": "npm run sync && cd mobile && npm run ios"
  },
  "dependencies": {
    "axios": "^1.7.7",
    "fs-extra": "^11.2.0"
  }
}
